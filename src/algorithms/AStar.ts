import { TileStatus } from "../enums/TileStatus";
import { Point } from "../interfaces/Point";

const AStar = (grid: TileStatus[][], origin: Point, destination: Point) => {
  console.log(grid);

  return [[], []];
};

export default AStar;
